{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pitwall/contract/driver/driver.history_appended.v1.schema.json",
  "title": "driver / driver.history_appended (v1) data payload",
  "description": "Driver stored one driver's per-session result. Published to the 'driver.events' exchange with routing key 'driver.history_appended'. Emitted ONE-PER-DRIVER when Driver consumes a timing/session.ended (Story 3.3, Q&A Round 36 / Q36.2): each row of session.ended.summary[] becomes its own driver.history_appended so a consumer (Frontend) can key its per-driver read-model update off masterId without unpacking an array. bestLapMs/lapCount mirror the stored summary row (contract/schemas/timing/session.ended.v1.schema.json's summary items); this is a session-scoped result, NOT the canonical all-time PR (that is driver.pr_updated, Story 3.4).",
  "type": "object",
  "required": ["masterId", "sessionId"],
  "properties": {
    "masterId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$",
      "description": "Canonical masterId issued by Identity. Lowercase canonical UUID v4 (version nibble 4, variant nibble [89ab]); the pattern is enforced (format is annotation-only)."
    },
    "sessionId": {
      "type": "string",
      "description": "The session this result belongs to. Same id space as lap.recorded.sessionId / session.ended.sessionId."
    },
    "bestLapMs": {
      "type": ["integer", "null"],
      "minimum": 1,
      "description": "The driver's best (fastest) valid lap this session, in milliseconds. Null if the driver logged no counted lap (mirrors the stored summary row); always present as a key (no omitempty, AR9)."
    },
    "lapCount": {
      "type": ["integer", "null"],
      "minimum": 0,
      "description": "Number of counted laps the driver logged this session (out-lap excluded). Null if unknown; always present as a key."
    }
  }
}
