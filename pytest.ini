[pytest]
# Contract-layer tests today; per-service unit/integration tests are added under
# services/<svc>/ as each service lands (Story 1.3+). Keep test discovery explicit.
testpaths = tests
python_files = test_*.py
addopts = -q
